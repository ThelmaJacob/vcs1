# Shared password Thelma types on the login screen
APP_PASSWORD=change-me
# Random 32-byte hex, signs the session cookie: openssl rand -hex 32
AUTH_SECRET=
# Powers the assistant (field drafting + portfolio questions)
ANTHROPIC_API_KEY=
# Postgres connection string (Supabase, Neon, Vercel Postgres).
# Leave empty locally to fall back to a JSON file in .data/
DATABASE_URL=
